name: progress

on:
  push:
    branches:
      - master

jobs:
  Progress:
      runs-on: ${{ matrix.os }}
      strategy:
        fail-fast: true
        matrix:
          version:
            - 1
          arch:
            - x64
          os:
            - ubuntu-latest
      steps:
        run: ./progress.sh
