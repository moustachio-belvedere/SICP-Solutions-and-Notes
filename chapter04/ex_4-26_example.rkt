;; "give an example of a situation where it might be useful to have unless available as a procedure rather than as special form."

;; only examples came across where `map` functional-style iterators are used.

